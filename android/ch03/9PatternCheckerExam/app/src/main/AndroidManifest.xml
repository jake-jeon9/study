<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a9_patterncheckerexam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--windowSoftInputMode : stateHidden : 화면 시작시 키패드 숨김 설정
        adjustPan : editText가 키패드에 가려질 경우, 위쪽으로 editText화면을 위로 밀어 올리는 설정
        stateVisible : 셋팅할 때  <requestFocus/> 를 해줘야 시작하자마자 키패드드 온 할 수 있음
        -->
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateVisible|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>